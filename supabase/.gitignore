# Supabase CLI local-dev artifacts. Never commit these.

# Linked-project info (project ref, anon/service keys cached locally).
.temp/

# Branch-database state for `supabase branches`.
.branches/

# Any env files for Edge Functions or local overrides.
.env
.env.*
!.env.example
